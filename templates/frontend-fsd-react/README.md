# Frontend FSD: React

This directory will contain the React starter organized with Feature-Sliced Design. Its first runnable implementation should start with the minimal `app -> pages -> shared` structure and demonstrate clear ownership, public APIs, API adaptation, and focused tests. Features, entities, or other layers should be extracted only after confirmed reuse gives them a stable boundary.

Status: scaffold only; no runnable project has been added.
