# Frontend FSD: React

This directory will contain the React starter organized with Feature-Sliced Design. Its implementation must demonstrate FSD layers, slice public APIs, one minimal user feature, API adaptation, and focused tests.

Status: scaffold only; no runnable project has been added.
