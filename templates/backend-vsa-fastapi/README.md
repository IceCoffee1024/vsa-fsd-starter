# Backend VSA: FastAPI

This directory will contain the Python FastAPI starter. Its implementation must express use-case slices and module boundaries in Python without copying framework-specific layering from the .NET templates.

Status: scaffold only; no runnable project has been added.
